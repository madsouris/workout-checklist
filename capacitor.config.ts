import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Workout Checklist',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
